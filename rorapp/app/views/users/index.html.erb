<h1>Пользователи аукциона</h1>
<p>В этом разделе можно просмотреть список всех пользователей нашего аукциона и даже немного его подправить, пока разработчик это позволяет :-)</p>

<table>
  <tr>
    <th>Имя пользователя</th>
    <th>Полное имя</th>
    <th>E-mail</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.name %></td>
    <td><%= user.fullname %></td>
    <td><%= user.email %></td>
    <td><%= link_to 'Подробнее...', user %></td>
    <td><%= link_to 'Править', edit_user_path(user) %></td>
    <td><%= link_to 'Удалить', user, confirm: 'Уверен?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Создать нового пользователя!', new_user_path %>
