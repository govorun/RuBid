<h1>Listing fops</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @fops.each do |fop| %>
  <tr>
    <td><%= link_to 'Show', fop %></td>
    <td><%= link_to 'Edit', edit_fop_path(fop) %></td>
    <td><%= link_to 'Destroy', fop, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Fop', new_fop_path %>
